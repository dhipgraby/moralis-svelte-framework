const { Sequelize } = require('sequelize');


// Option 2: Passing parameters separately (other dialects)
export const sequelize = new Sequelize('shortest', 'root', 'kennow', {
  host: 'localhost',
  dialect:'mysql',
  port:3308 
});