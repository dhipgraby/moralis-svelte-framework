export function metamaskMessage(message) {

    if(message.message != undefined){
      return message.message
    } 

    return message
    
}

export function alertParser(message, alertType) {

    var alertData = {
        show: true,
        alert: alertType,
        content: message
    }

    return alertData

}

