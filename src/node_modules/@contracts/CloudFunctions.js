import { Moralis } from "@moralis/MoralisSvelte";

export async function bornDragons() {
    const query = new Moralis.Query('bornDragons');
    query.limit(25);
    
    const results = await query.find();
    return results
}